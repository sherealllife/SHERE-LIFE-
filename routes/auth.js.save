            process.env.JWT_SECRET, 
            { expiresIn: 
            process.env.JWT_EXPIRE 
            || '7d' }

        res.json({ success: true, 
            message: 'Login 
            success', token, 
            user: {
                id: user._id, 
                name: user.name, 
                email: 
                user.email, role: 
                user.role
